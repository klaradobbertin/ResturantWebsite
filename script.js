// Select the element with id "dryckSection"
const dryckSection = document.getElementById("dryckSection");

// Check if the element is found

if (dryckSection) {
    // Change the background image
    dryckSection.style.backgroundImage = "url('drink.jpg')";
}
